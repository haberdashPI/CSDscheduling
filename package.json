{
  "name": "CSDschedule",
  "private": true,
  "version": "0.0.1",
  "description": "Simple schedule UI",
  "devDependencies": {
    "bower": "^1.3.1",
    "shelljs": "^0.2.6"
  },
  "scripts": {
    "postinstall": "bower install",

    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1",

    "pretest": "npm install"
  }
}
